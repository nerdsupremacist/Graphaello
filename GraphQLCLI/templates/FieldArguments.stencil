{% for argument in field.arguments %}{{ argument.name }}: GraphQLArgument<{{ argument.type|swiftType }}> {% if argument.defaultValue %}= .argument(argument.defaultValue){% else %}= .argument{% endif %}{% if not forloop.last %},{% endif %}{% endfor %}
