{% if hasArguments %}
{% if isStatic %}static {% endif %}func {{field.name}}({{ field.arguments|codeArray|join:", " }}) -> {{ pathClass }}<{{ swiftType }}> {
    return .init()
}
{% endif %}
{% if isStatic %}static {% endif %}var {{field.name}}: {{ pathClass }}<{{ swiftType }}> { .init() }
